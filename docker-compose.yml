services:
  app:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 80
    ports:
      - "3000:80"
    volumes:
      - cv-data:/app/data

volumes:
  cv-data:
